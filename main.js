const navLinks = document.getElementById("navLinks");

// Show & Hide Menu by styling right css value of nav links
function showMenu() {
    navLinks.style.right = "0";
}

function hideMenu() {
    navLinks.style.right = "-400px";
}

